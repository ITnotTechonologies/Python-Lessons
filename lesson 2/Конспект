Типы Данных
Гитхаб 


Функция print может выводить некое значение переменной, нексолько переменных можно вывести через пробел



Для того, чтобы вывести некую строку, она берется в кавычки 

Сепараторы - специальный знаки(или строки) <значения>, которые будут между переменными

Операторы

Оператор     Значение
	+		 сложение
	*		 вычитание
	**		 возведение в степень
	%	  	 остаток от деления
	//		 целочисленное деление
	/		 деление
	-		 вычитание

функции вычисления 

функция     Значение
	abs		 модуль числа


...а дальше ручками писать

Типы данных		Пример

...
	кортеж			неизменяемые
					(4, 'e - ', 5, 'e class') 
	множество(set)   {123, 321, 123}		


